import 'package:app/main.dart';
import 'package:test/test.dart';

void main() {
  test('Test if the while is running', () {
    expect(valid, equals(false));
  });

  test('Test if message start empty', () {
    expect(message, equals(''));
  });
}
